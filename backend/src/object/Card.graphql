type Card {
    id: Int!
    cardType: String!
    title: String!
    description: String!
    displayOrder: Int!
    subTitle: String
    imagePath: String
    technologies: [String]
}

type Query {
    card(id: Int): Card,
    cards: [Card],
}
